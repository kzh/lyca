func () > main > (int) {
    Object p = make Object < ();
    Object o = createObj();

    return o.id;
}

func () > createObj > (Object) {
    Object o = make Object < ();

    return o;
}

tmpl Object {
    int id;

    constructor < () {
        this.id = 5;
    }
}
